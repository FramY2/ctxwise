interface:
  display_name: "CtxWise"
  short_description: "See what Codex carries before a task"
  default_prompt: "Use $ctxwise to show my immediate context snapshot and explain the safest improvement."
policy:
  allow_implicit_invocation: false
